<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Mandatory_Fields</fullName>
    <active>false</active>
    <errorConditionFormula>AND(RecordType.DeveloperName == &apos;Idea&apos;,IF(ISBLANK(TEXT( MoSCoW_Value__c)),true,
   IF(ISBLANK(Regions_Impacted__c),true,   
     IF(ISBLANK(TEXT( Requesting_organization__c)),true,   
        IF(ISBLANK(TEXT( Requesting_region__c )),true,
           IF(LEN( Business_Challenge__c)== 0,true,
   IF(LEN( Business_Justification__c ) == 0,true,
		IF(LEN( Business_Objective__c ) == 0,true,false)
	 )
)
		  )
		)
	  )
   ))</errorConditionFormula>
    <errorMessage>The following fields are mandatory : MoSCoW Value, Regions Impacted, Requesting Organization , Requesting Region, Business Challenge, Business Justification &amp; Business Objective</errorMessage>
</ValidationRule>
