@isTest
public class CloneSupportRequestTest {
    @isTest Static void TestCloneRecord(){
        Test.startTest();
        CloneSupportRequest.getDetailsFromOpp(null);
        Test.stopTest();
        System.assert(True);
    }
}